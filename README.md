# ASP-E2-Capability-Skill-Declaration-Layer
A machine‑only extension of ASP that adds a structured capability and skill declaration map. This allows agents to advertise their tools, domains, competencies, limits, and operational boundaries in a deterministic format for downstream routing and task matching.
